'use client';
import {AdminFrame} from '../components/admin-frame';import {ResourceTable} from '../components/resource-table';import {money,when} from '../lib/admin-api';
export default function Page(){return <AdminFrame title="Ride requests" subtitle="Customer demand, driver response and marketplace conversion."><ResourceTable endpoint="/api/v1/admin/operations/ride-requests" title="Ride requests" searchKeys={["customerName", "pickupLabel", "destinationLabel"]} columns={[{key:'customerName',label:'Customer'},{key:'pickupLabel',label:'Pickup'},{key:'destinationLabel',label:'Destination'},{key:'pickupAt',label:'Pickup time',render:r=>when(r.pickupAt)},{key:'customerOffer',label:'Offer',render:r=>money(r.customerOffer)},{key:'offerCount',label:'Driver offers'},{key:'status',label:'Status'}]}/></AdminFrame>}
