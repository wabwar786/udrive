<?xml version="1.0" encoding="utf-8"?>
<!--
    android:roundIcon in the manifest points here. An adaptive icon is already
    masked by the launcher, so the round variant is the same three layers; the
    rounded PNGs in mipmap-*dpi are what pre-Android-8 launchers use.
-->
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/ic_launcher_background" />
    <foreground android:drawable="@drawable/ic_launcher_foreground" />
    <monochrome android:drawable="@drawable/ic_launcher_monochrome" />
</adaptive-icon>
