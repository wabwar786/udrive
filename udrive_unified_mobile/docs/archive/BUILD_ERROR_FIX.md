# Flutter Web Build Error Fix

Fixed the Dart syntax error in:

`udrive_unified_mobile/lib/screens/customer/customer_home_screen.dart`

The `rides.map(...)` widget spread had an extra closing parenthesis near the Available rides section. It has been rewritten in a clear multiline format with balanced brackets.
