'use client';
import {useEffect,useState} from 'react';import Link from 'next/link';import {ArrowUpRight,BadgeCheck,BookOpenCheck,Car,PackageCheck,ShieldAlert} from 'lucide-react';import {AdminFrame} from './components/admin-frame';import {ErrorBox,Loading,Stat,Badge} from './components/ui';import {apiFetch,money,when} from './lib/admin-api';
type Data={metrics:{label:string;value:number;changePercent:number;tone:string}[];recentActivity:{type:string;title:string;subtitle:string;occurredAt:string;status:string}[];bookingStatuses:Record<string,number>;verificationQueues:Record<string,number>};
export default function Dashboard(){const[data,setData]=useState<Data|null>(null);const[error,setError]=useState('');useEffect(()=>{apiFetch<Data>('/api/v1/admin/operations/dashboard').then(setData).catch(e=>setError(e.message))},[]);return <AdminFrame title="Operations overview" subtitle="Live tourism marketplace, traveller safety and verification health.">{error&&<ErrorBox message={error}/>} {!data?<Loading/>:<><section className="statGrid">{data.metrics.map(m=><Stat key={m.label} label={m.label} value={m.label.includes('value')?money(m.value):m.value.toLocaleString()} tone={m.tone}/>)}</section><section className="dashboardGrid"><article className="panel"><header className="panelHeader"><div><h2>Recent activity</h2><p>Latest operational events</p></div><Link className="textLink" href="/bookings">View bookings <ArrowUpRight/></Link></header><div className="activityList">{data.recentActivity.map((a,i)=><div key={`${a.title}-${i}`}><span className="activityDot"/><div><strong>{a.title}</strong><p>{a.subtitle}</p></div><div><Badge value={a.status}/><small>{when(a.occurredAt)}</small></div></div>)}</div></article><article className="panel"><header className="panelHeader"><div><h2>Action centre</h2><p>Items requiring your team</p></div></header><div className="actionCards"><Link href="/verification"><BadgeCheck/><div><strong>{data.verificationQueues.drivers??0} Drivers</strong><span>Waiting for verification</span></div></Link><Link href="/verification"><Car/><div><strong>{data.verificationQueues.vehicles??0} Vehicles</strong><span>Documents to review</span></div></Link><Link href="/packages"><PackageCheck/><div><strong>{data.verificationQueues.packages??0} Packages</strong><span>Tour approvals</span></div></Link><Link href="/safety"><ShieldAlert/><div><strong>Safety centre</strong><span>Escalate open incidents</span></div></Link></div></article></section><section className="panel"><header className="panelHeader"><div><h2>Booking pipeline</h2><p>Current lifecycle distribution</p></div></header><div className="pipeline">{Object.entries(data.bookingStatuses).map(([status,count])=><div key={status}><BookOpenCheck/><strong>{count}</strong><span>{status}</span></div>)}</div></section></>}</AdminFrame>}
